object类是所有类的父类
java.lang中的类是不需要导入的，默认自动导入，其他类使用时需要手动导入
对象的hashcode每次运行会变，且每个对象都不同，而值一样的变量hashcode也一样
tostring
可以重写tostring来覆盖父类的tostring方法，输出其他格式
equals是比较两个对象值是否相等
==是比较指向的内存是否相等 
比较两个对象是否相等，首先要覆盖equals还要覆盖hashcode
数组
一个变量存储一系列同类型数据